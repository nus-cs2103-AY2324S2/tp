package seedu.address.ui;

/**
 * Enum representing different viewing modes that application should be in after command execution.
 */
public enum ViewMode {
    OVERALL,
    DAY,
    SWITCH,
    ANY
}
